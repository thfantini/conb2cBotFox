{
	"table": "v_boleto_mes",
	"rows":
	[
		{
			"cliente": 21,
			"cnpj": "02.968.465/0001-66",
			"nome": "LABORATORIO DE ANALISES CLINICAS DR. MIGUEL LTDA.",
			"celular": "5537991458954",
			"nfse": 190037,
			"conta": 210443,
			"empNome": "AMBIENTEC SOLUCOES EM RESIDUOS LTDA",
			"empCNPJ": "11.399.773/0001-09",
			"dataDoc": "2025-09-02 07:50:45.377",
			"dataVencimento": "2025-09-28 00:00:00.000",
			"numero": "210443-1",
			"valor": 157.79,
			"codBarras": "75692121800000157791314001099317418746595001",
			"linhaDigitavel": "75691.31407 01099.317412 87465.950019 2 12180000015779"
		}
	]
}

DIRETRIZES
1 - não desenvolva nenhum código sem que antes eu autorize a criação;
2 - não altere nenhuma funcionalidade existente sem autorização;
3 - não crie novas funcionalidades sem autorização;
4 - não crie um front-end;
5 - crie um arquivo por vez, aguarde autorização para criar o proximo;
6 - todos os componentes devem ser dinâmicos e receber as informações atraves das props;
7 - nenhuma informação deve ser mocada, todas serao disponibilizadas atraves do banco de dados;
8 - siga sempre o mesmo padrao de codigo sujerido no repositorio referencia.

---------------------------------------------

vw_boletos > vw_botboletos
SELECT
whapi_clientes_boleto.cliente,
whapi_clientes_boleto.cnpj,
whapi_clientes_boleto.nome,
whapi_clientes_boleto.celular,
whapi_clientes_boleto.email,
whapi_clientes_boleto.idConta,
whapi_clientes_boleto.idNfse,
whapi_clientes_boleto.dataDoc,
whapi_clientes_boleto.dataVencimento,
whapi_clientes_boleto.numero,
whapi_clientes_boleto.valor,
whapi_clientes_boleto.codBarras,
whapi_clientes_boleto.linhaDigitavel
FROM whapi_clientes_boleto
WHERE whapi_clientes_boleto.status = 0

---------------------------------------------

SELECT
whapi_clientes_boleto.cliente,
whapi_clientes_boleto.cnpj,
whapi_clientes_boleto.nome,
whapi_clientes_boleto.celular,
whapi_clientes_boleto.email,
whapi_clientes_boleto.idConta,
whapi_clientes_boleto.idNfse,
whapi_clientes_boleto.dataDoc,
whapi_clientes_boleto.dataVencimento,
whapi_clientes_boleto.numero,
whapi_clientes_boleto.valor,
whapi_clientes_boleto.codBarras,
whapi_clientes_boleto.linhaDigitavel,
CONCAT(aux_app.url,whapi_clientes_boleto.idConta,'/') as url,
whapi_clientes_boleto.`status`
FROM whapi_clientes_boleto
LEFT JOIN aux_app ON aux_app.cliente = whapi_clientes_boleto.cliente
WHERE whapi_clientes_boleto.status = 0 


---------------------------------------------

CREATE TABLE IF NOT EXISTS `aux_app` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `nome` varchar(100) DEFAULT NULL,
  `cliente` int(11) DEFAULT 0,
  `status` int(11) DEFAULT 0,
  `data_inicio` date DEFAULT NULL,
  `data_fim` date DEFAULT NULL,
  `hora_inicio` time DEFAULT NULL,
  `hora_fim` time DEFAULT NULL,
  `sinc_data` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=2 DEFAULT CHARSET=latin1 COLLATE=latin1_general_ci;







